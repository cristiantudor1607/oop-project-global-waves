package globalwaves.commands.enums;

public class AddRemoveExit {
    public enum code {
        NOT_A_SONG,
        INVALID_PLAYLIST,
        NO_SOURCE,
        ADDED,
        REMOVED,
    }

    private AddRemoveExit() {}
}
