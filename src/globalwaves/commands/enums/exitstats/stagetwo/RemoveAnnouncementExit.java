package globalwaves.commands.enums.exitstats.stagetwo;

public class RemoveAnnouncementExit {
    public enum Status {
        DOESNT_EXIST,
        NOT_HOST,
        INVALID_NAME,
        SUCCESS,
    }
}
