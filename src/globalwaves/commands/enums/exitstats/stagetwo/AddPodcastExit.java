package globalwaves.commands.enums.exitstats.stagetwo;

public class AddPodcastExit {
    public enum Status {
        DOESNT_EXIST,
        NOT_HOST,
        SAME_NAME,
        DUPLICATE,
        SUCCESS,
    }
}
