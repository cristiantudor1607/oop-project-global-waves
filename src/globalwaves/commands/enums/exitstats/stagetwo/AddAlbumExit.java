package globalwaves.commands.enums.exitstats.stagetwo;

public class AddAlbumExit {
    public enum Status {
        INVALID_USERNAME,
        NOT_ARTIST,
        SAME_NAME,
        SAME_SONG,
        SUCCESS,
    }
}
