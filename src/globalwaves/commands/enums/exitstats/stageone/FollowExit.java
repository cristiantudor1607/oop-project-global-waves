package globalwaves.commands.enums.exitstats.stageone;

public class FollowExit {
    public enum Status {
        FOLLOWED,
        UNFOLLOWED,
        NOT_A_PLAYLIST,
        NO_SOURCE,
        OWNER,
        OFFLINE,
    }
}
