package globalwaves.commands.enums.exitcodes.stageone;

public class FollowExit {
    public enum Status {
        FOLLOWED,
        UNFOLLOWED,
        NOT_A_PLAYLIST,
        NO_SOURCE,
        OWNER,
    }
}
